name: github_pages

on:
  push:
    branches:
      - main

jobs:
  github_pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: /home/runner/work/medium_scraper/medium_scraper/output/bi
